apiVersion: v2
name: goat-yard-archive
description: Deployment chart for Goat Yard Archive (Frontend + Backend)
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: minio
    version: 14.6.0 # Check for latest stable
    repository: https://charts.bitnami.com/bitnami
    condition: minio.enabled
