apiVersion: v2
name: goat-yard-archive
description: Deployment chart for Goat Yard Archive (Frontend + Backend)
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: minio
    version: 14.8.0 # Updating to newer chart to fix image tag issue
    repository: https://charts.bitnami.com/bitnami
    condition: minio.enabled
