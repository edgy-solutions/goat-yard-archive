[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gill_commentary_pipeline"
version = "0.1.0"
dependencies = [
    "dagster",
    "dagster-webserver",
]

[tool.dagster]
module_name = "gill_commentary_pipeline"
